{
	"name": "emissor-nfcom",
	"type": "module",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"imports": {
		"#/*": "./apps/backend/src/*"
	},
	"scripts": {
		"dev": "docker compose -f docker-compose.dev.yaml up",
		"dev:native": "bun --watch apps/backend/src/index.ts",
		"build": "bun build apps/backend/src/index.ts --outdir dist --target bun",
		"viewer:dev": "bun run --cwd apps/viewer dev",
		"viewer:build": "bun run --cwd apps/viewer build",
		"start": "bun run apps/backend/src/index.ts",
		"test": "bun test --isolate",
		"test:watch": "bun test --isolate --watch",
		"test:coverage": "bun test --isolate --coverage",
		"lint": "biome lint .",
		"typecheck": "tsc --noEmit --types bun",
		"format:md": "bunx prettier --write \"**/*.{md,mdx}\" --no-error-on-unmatched-pattern --cache",
		"format:sh": "bunx prettier --write \"**/*.sh\" --plugin=prettier-plugin-sh --no-error-on-unmatched-pattern --cache",
		"format": "biome format --write . && bun format:md && bun format:sh",
		"knip": "knip",
		"prepare": "husky",
		"spellcheck": "cspell .",
		"secrets:check": "sh scripts/gitleaks-check",
		"lint-staged": "lint-staged",
		"generate:types": "bun ./scripts/nocobase/index.ts",
		"db:generate": "bunx drizzle-kit generate",
		"db:migrate": "docker compose -f docker-compose.dev.yaml exec --user bun app bunx drizzle-kit migrate",
		"db:studio": "docker compose -f docker-compose.dev.yaml exec --user bun app bunx drizzle-kit studio --host 0.0.0.0",
		"db:push": "docker compose -f docker-compose.dev.yaml exec --user bun app bunx drizzle-kit push",
		"db:check": "docker compose -f docker-compose.dev.yaml exec --user bun app bunx drizzle-kit check",
		"db:drop": "docker compose -f docker-compose.dev.yaml exec --user bun app bunx drizzle-kit drop",
		"db:introspect": "docker compose -f docker-compose.dev.yaml exec --user bun app bunx drizzle-kit introspect"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.8",
		"@cspell/dict-pt-br": "^2.4.2",
		"@libsql/client": "^0.17.4",
		"@t3-oss/env-core": "^0.13.11",
		"@types/bun": "^1.3.14",
		"cspell": "^10.0.1",
		"dotenv": "^17.4.2",
		"drizzle-kit": "^0.31.10",
		"husky": "^9.1.7",
		"knip": "^6.32.2",
		"lint-staged": "^17.3.0",
		"listr2": "^11.0.0",
		"prettier": "^3.9.6",
		"prettier-plugin-sh": "^0.19.0",
		"tsc-files": "^1.1.4",
		"typescript": "^7.0.2",
		"zod": "^4.4.3"
	}
}
