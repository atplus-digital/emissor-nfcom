# Backlog

Ledger de reservas de ID para ADRs e SPECs (ADR-0013 · `scripts/docs-reserve`).
O próximo número é `max(arquivos em docs/<camada>/, NNNNs da tabela) + 1`;
número liberado à mão nunca é reutilizado (renumerar cascateia, STANDARD §5).

## Reservas ADR

| NNNN | título | data | situação |
| --- | --- | --- | --- |

## Reservas Spec

| NNNN | título | data | situação |
| --- | --- | --- | --- |
| 0003 | Cancelamento de NFCom e descarte de fatura | 2026-08-14 | reservada (endpoint confirmado: `DELETE /api/cancela?chave=&protocolo=`) |
